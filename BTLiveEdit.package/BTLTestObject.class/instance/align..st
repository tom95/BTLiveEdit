as yet unclassified
align: aMode
	<liveEditable>
	
	align := aMode.
	
	"context := thisContext sender.
	[context selector == #DoIt] whileFalse: [context := context sender].
	context sender explore.
	(context sender at: 1) sourceStream contents explore"